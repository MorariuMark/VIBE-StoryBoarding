# HandScribe: every install stays inside the project folder — no C: leftovers.
# npm resolves this file from the project root, so ./ paths land in D:\VIBE-StoryBoarding.
cache=./.cache/npm
fund=false
audit=false
