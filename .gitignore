node_modules/
dist/
dist-ssr/
*.local
.DS_Store
# Project-local AI assets (gigabytes — re-created by scripts/setup-audio8.bat)
/models/
/server/python/.venv/
/server/python/*.log
# Project-local runtime/caches (never commit, never C:)
# - .cache/      npm + pip + script temp (pinned by .npmrc / setup bat)
.cache/
# - .user-data/  Electron profile (redirected in electron/main.cjs)
.user-data/
